@* Copyright (c) 2023 Xomega.Net. All rights reserved. *@

@inherits XComponent

@if (IsVisible)
{
    <span @attributes="AdditionalAttributes" class="@StateClass">
        @(Property == null ? "" : Property.GetStringValue(ValueFormat.DisplayString, Row))
    </span>
}

@code {
    protected override void OnInitialized()
    {
        ObservedChanges = PropertyChange.Value + PropertyChange.Visible;
    }
}