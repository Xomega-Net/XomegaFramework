<!--
 =============================================================================================
  This file was AUTO-GENERATED by "WPF Views" Xomega.Net generator.
 
  Manual CHANGES to this file WILL BE LOST when the code is regenerated.
 =============================================================================================
-->
<xov:WPFDetailsView xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:xov="clr-namespace:Xomega.Framework.Views;assembly=Xomega.Framework.Wpf"
                    xmlns:o="clr-namespace:AdventureWorks.Client.Objects;assembly=AdventureWorks.Client.Common"
                    xmlns:xom="clr-namespace:Xomega.Framework;assembly=Xomega.Framework.Wpf"
                    x:Class="AdventureWorks.Client.Wpf.LoginView"
                    ViewTitle="Login"
                    ViewWidth="300"
                    ViewHeight="180">
  <DockPanel Margin="10">
    <DockPanel DockPanel.Dock="Bottom">
      <Button x:Name="btnClose" Width="75" Margin="0,10,0,0" Content="_Close"
              DockPanel.Dock="Right"/>
      <Button x:Name="btnSave" Width="75" Margin="10,10,10,0" Content="_Save"
              HorizontalAlignment="Right"/>
    </DockPanel>
    <GroupBox Header="Login" DockPanel.Dock="Top">
      <StackPanel>
        <Grid>
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
          </Grid.RowDefinitions>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
          </Grid.ColumnDefinitions>
          <Label Grid.Row="0" Grid.Column="0" Name="lblEmail"
                 Style="{StaticResource LabelStyle}">_Email:</Label>
          <TextBox Grid.Row="0" Grid.Column="1"
                   xom:Property.Label="{Binding ElementName=lblEmail}"
                   x:Name="ctlEmail"
                   xom:Property.Name="{x:Static o:AuthenticationObject.Email}"
                   Style="{StaticResource ControlStyle}"/>
          <Label Grid.Row="1" Grid.Column="0" Name="lblPassword"
                 Style="{StaticResource LabelStyle}">_Password:</Label>
          <PasswordBox Grid.Row="1" Grid.Column="1"
                       xom:Property.Label="{Binding ElementName=lblPassword}"
                       x:Name="ctlPassword"
                       xom:Property.Name="{x:Static o:AuthenticationObject.Password}"
                       Style="{StaticResource ControlStyle}"/>
        </Grid>
      </StackPanel>
    </GroupBox>
  </DockPanel>
</xov:WPFDetailsView>